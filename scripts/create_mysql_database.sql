-- Run this in phpMyAdmin SQL tab or via MySQL CLI to create the application database

CREATE DATABASE IF NOT EXISTS `3c_gadget_hub`
  CHARACTER SET = 'utf8mb4'
  COLLATE = 'utf8mb4_unicode_ci';

-- Optional: create a dedicated user (change 'devuser' and password)
-- CREATE USER 'devuser'@'localhost' IDENTIFIED BY 'your_password_here';
-- GRANT ALL PRIVILEGES ON `3c_gadget_hub`.* TO 'devuser'@'localhost';
-- FLUSH PRIVILEGES;
